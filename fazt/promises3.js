function makeQueryDatabase() {
  // setTimeout(() => {
  //   console.log("MAKING QUERY");
  // }, 5000);

  for (let i = 0; i < 1000; i++) {
    return ""
  }
}

function uploadPhoto() {
  console.log("UPLOADING PHOTO");
}
function uploadVideo() {
  console.log("UPLOADING VIDEO");
}
function uploadPost() {
  console.log("UPLOADING POST");
}

async function main() {
  await makeQueryDatabase();
  uploadPhoto();
  uploadPost();
  uploadVideo();
  uploadVideo();
  uploadVideo();
  uploadVideo();
  uploadVideo();
  uploadVideo();
  uploadVideo();
  uploadVideo();
  uploadVideo();
  uploadVideo();
  uploadVideo();
  uploadVideo();
  uploadVideo();
  uploadVideo();
}

await main();
